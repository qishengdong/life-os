# dependencies
node_modules/
.pnp
.pnp.js

# testing
coverage/

# next.js
.next/
out/

# production
build/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env (CRITICAL - never commit)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# data (CRITICAL - never commit user data)
/data/
*.db
*.db-journal
*.db-wal
*.db-shm

# typescript
*.tsbuildinfo
next-env.d.ts

# IDE
.vscode/
.idea/
.cursor/

# Claude Code: 共享 settings.json, 但忽略 local 覆盖和缓存
.claude/*
!.claude/settings.json
.claude/settings.local.json

# OS
Thumbs.db

# Design handoff: 大体积 deliverables (bundled HTML / fonts / 解包临时) 不进版本控制
# 保留 4 个 brief 子目录 + ITERATIONS markdown
design-handoff/DELIVERABLES/_unpacked/
design-handoff/DELIVERABLES/**/*.woff2
design-handoff/DELIVERABLES/**/*.woff
design-handoff/DELIVERABLES/KEY V1 Handoff.html
design-handoff/DELIVERABLES/KEY/KEY V1 Handoff.html
design-handoff/DELIVERABLES/*.zip
design-handoff/*.zip
